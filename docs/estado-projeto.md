# Estado do Mercadeira

Atualizado em 13/09/2026. Registro de retomada; README documenta o produto e GitHub Project mantém o backlog.

- **Repositórios:** frontend em `C:\Users\leoaf\projetos\mercadeira-frontend`; backend em `C:\Users\leoaf\projetos\mercadeira.api`.
- **Backlog:** https://github.com/users/lnrdgst/projects/1 — consultar prioridade, status e dependências. Melhorias reais fora do escopo devem ser pesquisadas nos dois repositórios e Project; sem equivalente, criar Issue real no repositório correto e adicioná-la ao Project em Backlog, com labels existentes e prioridade quando clara. Autorização operacional concedida pelo usuário; não registrar detalhes triviais ou refatorações preferenciais.
- **Produto:** fluxo de autenticação, família, preparação de listas e Compra até revisão/finalização implementado. Último marco concluído: Compra 4, com os seis cenários manuais aprovados pelo usuário em 13/09/2026; detalhes em `marco-compra-4.md`.
- **Trabalho atual:** BE #1 → FE #2 concluídas: edição de nome, categoria e estabelecimento por capability, com contrato, integração e testes. Validação manual aprovada pelo usuário após correção da mensagem para nome vazio; commit/push dos dois repositórios autorizado.
- **Contrato:** backend docs/contrato-edicao-lista.md. PUT /api/familias/{familiaId}/listas/{listaId}; somente criador ou administrador com vínculo ativo, em EM_PREPARACAO. Participação isolada não permite editar. contextoUsuario.podeEditarDadosBasicos é a única autorização usada pela UI. Lock da ListaCompra serializa edição e início; GET reconcilia 403/409. Sem migration.
- **Decisões:** backend define domínio, autorização e capabilities; frontend consome respostas completas. Restauração usa POST sem body, 200 com ItemCompra completo, capability exclusiva, auditorias preservadas e GET em 409. Revisão/finalização não oferecem mutações de itens.
- **Evidências:** backend: 10 novos testes específicos e suíte completa de 315 testes aprovados, BUILD SUCCESS; concorrência nas duas ordens observada por lock no PostgreSQL. Frontend: 69 testes aprovados (58 anteriores + 11 novos), build/lint aprovados. Diff-check dos dois repositórios aprovado na retomada; ajuste manual 2.3: nome vazio agora mostra “Informe o nome da lista.” no formulário, suprimindo o aviso nativo; teste de bloqueio e recuperação adicionado, suíte frontend/build/lint reexecutados e aprovados. Aviso conhecido de shutdown do Surefire segue FE #11, sem nova Issue.
- **Validação manual:** usuário confirmou aprovação dos cenários de edição pelo criador/administrador, bloqueio ao participante comum, cancelamento, nome obrigatório em português, persistência/F5 e conflito com início em outra sessão. Evidência manual fornecida pelo usuário; testes frontend automatizados usam HTTP simulado.
- **Próxima tarefa obrigatória:** gate final do piloto em plano-v1.md, após o checkpoint da edição. FE #12 permanece Backlog / Média; Histórico dedicado fora da V1. Conferência com leitor de tela da FE #1 permanece no gate final.
- **Controle:** commit/push na main dos dois repositórios autorizado após testes manuais aprovados. Checkpoints separados; após push confirmado, encerrar BE #1/FE #2 e marcar Concluído no Project. Manter critérios gerais de validação e escopo para checkpoints; sem operações destrutivas, force push ou merge sem autorização específica.
